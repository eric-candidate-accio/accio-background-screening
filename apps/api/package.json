{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "bundle install",
    "dev": "bundle exec ruby myapp.rb",
    "build": "echo 'No build step for Ruby'",
    "start": "bundle exec ruby myapp.rb -e production",
    "lint": "bundle exec rubocop || true"
  }
}
