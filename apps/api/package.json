{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "eval \"$(rbenv init -)\" && bundle install",
    "dev": "eval \"$(rbenv init -)\" && bundle exec ruby myapp.rb",
    "build": "echo 'No build step for Ruby'",
    "start": "eval \"$(rbenv init -)\" && bundle exec ruby myapp.rb -e production",
    "lint": "eval \"$(rbenv init -)\" && bundle exec rubocop || true"
  }
}
